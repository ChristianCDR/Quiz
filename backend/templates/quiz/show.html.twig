{% extends 'base.html.twig' %}

{% block title %}Supprimer{% endblock %}

{% block body %}
    <h1>Quiz N°{{ question.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Question:</th>
                <td>{{ question.questionText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_index') }}">back to list</a>

    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
