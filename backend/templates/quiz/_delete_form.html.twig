<form method="post" action="{{ path('app_quiz_delete', {'id': question.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette ligne?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.id) }}">
    <button class="btn">Supprimer</button>
</form>
 